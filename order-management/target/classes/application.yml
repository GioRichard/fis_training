server:
  port: 8080
spring:
  application:
    name: Order management application
  datasource:
    url: jdbc:mysql://localhost:3306/fis-training?allowPublicKeyRetrieval=true&useSSL=false
      username: root
      password: root123
  jpa:
      show-sql: true
      generate-ddl: true